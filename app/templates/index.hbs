
{{group-controller group=groups allLights=allLights update="updateModel"}}

<div class='side-bar'>
  {{#each lights.lights as |light|}}
    {{light-controller light=light update="updateModel"}}
  {{/each}}
</div>
